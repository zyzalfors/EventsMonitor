# Events Monitor 1.0.4
Events Monitor 1.0.4 is .NET Win Forms GUI programs that allows to monitor clipboard, mouse, keyboard and filesystem events. It uses wrapped .NET and native WIN32 functionalities to implement events monitoring
