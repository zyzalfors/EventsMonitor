# Events Monitor 1.0.6
Events Monitor is a .NET Win Forms GUI program that allows to monitor clipboard, mouse, keyboard and filesystem events. It uses wrapped .NET and native WIN32 functionalities to implement events monitoring
